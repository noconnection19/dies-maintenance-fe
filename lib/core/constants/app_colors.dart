import 'package:flutter/material.dart';

/// Semua konstanta warna aplikasi, dipusatkan di satu tempat.
class AppColors {
  AppColors._();

  static const background    = Color(0xFFF8FAFC);
  static const surface       = Colors.white;
  static const textPrimary   = Color(0xFF1E293B);
  static const textSecondary = Color(0xFF64748B);
  static const textMuted     = Color(0xFF94A3B8);
  static const green         = Color(0xFF059669);
  static const greenLight    = Color(0xFFD1FAE5);
  static const divider       = Color(0xFFE2E8F0);

  // Card accent colors
  static const lineStop   = Color(0xFFEF4444); // red
  static const repair     = Color(0xFF3B82F6); // blue
  static const preventive = Color(0xFFF59E0B); // amber
  static const error      = Color(0xFFEF4444); // red / error states
}
